import { HardwareBay } from "@/components/hardware-bay";

export const metadata = {
  title: "Hardware Bay",
  description: "Named silicon, isolation policy, and the Quest path that actually worked.",
};

export default function HardwarePage() {
  return <HardwareBay />;
}
