import { CheckCircle2, CircleDashed, Wrench } from "lucide-react";
import { toolRegistry } from "@/lib/tools";

export default function ToolsPage() {
  return <div className="flex flex-col gap-6"><section><p className="eyebrow">Integration runway</p><h1 className="font-heading text-4xl font-semibold">Tool Bay</h1><p className="mt-2 max-w-2xl text-base leading-7 text-muted-foreground">Interfaces first. External access only when its boundary is explicit, boring, and testable.</p></section><div className="grid gap-3 md:grid-cols-2">{toolRegistry.map((tool) => <article key={tool.callsign} className="rounded-xl bg-card/85 p-5 ring-1 ring-foreground/10"><div className="flex items-start justify-between gap-3"><div><p className="eyebrow">{tool.callsign}</p><h2 className="font-heading mt-1 text-xl">{tool.name}</h2></div><span className={`flex items-center gap-1 rounded-full border px-2 py-1 text-xs uppercase ${tool.status === "online" ? "border-signal/40 text-signal" : tool.status === "ready" ? "border-primary/40 text-primary" : "border-border text-muted-foreground"}`}>{tool.status === "online" ? <CheckCircle2 className="size-3" /> : tool.status === "ready" ? <Wrench className="size-3" /> : <CircleDashed className="size-3" />}{tool.status}</span></div><p className="mt-3 text-sm leading-6 text-muted-foreground">{tool.purpose}</p><div className="mt-4 border-l border-primary/40 pl-3"><p className="text-xs uppercase tracking-wider text-muted-foreground">Next interface</p><p className="mt-1 text-sm">{tool.next}</p></div></article>)}</div></div>;
}
